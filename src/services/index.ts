import * as recipes from "./ingredientsService"
import * as ingredientes from "./ingredientsService"
import * as preparations from "./preparationService"

export const services = {
  recipes,
  ingredientes,
  preparations,

  storage: {
    imagePath: "",
  },
}